<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/grass_background_normal">

    <Button
        android:id="@+id/btnExit"
        android:layout_width="300dp"
        android:layout_height="150dp"
        android:background="@drawable/custom_button_large_exit"
        android:layout_marginTop="15dp"
        android:layout_below="@+id/btnSettings"
        android:layout_alignLeft="@+id/btnSettings"
        android:layout_alignStart="@+id/btnSettings" />

    <Button
        android:id="@+id/btnSettings"
        android:layout_width="300dp"
        android:layout_height="150dp"
        android:layout_marginTop="15dp"
        android:background="@drawable/custom_button_large_settings"
        android:layout_below="@+id/btnPlay"
        android:layout_alignLeft="@+id/btnPlay"
        android:layout_alignStart="@+id/btnPlay" />

    <Button
        android:id="@+id/btnPlay"
        android:layout_width="300dp"
        android:layout_height="150dp"
        android:background="@drawable/custom_button_large_play"
        tools:layout_editor_absoluteY="142dp"
        tools:layout_editor_absoluteX="56dp"
        android:layout_marginTop="51dp"
        android:layout_below="@+id/imageView3"
        android:layout_centerHorizontal="true" />

    <android.support.v7.widget.AppCompatImageView
        android:id="@+id/imageView3"
        android:layout_width="487dp"
        android:layout_height="365dp"
        app:srcCompat="@mipmap/football_logo_quiz_text_normal"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginTop="26dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

</RelativeLayout>